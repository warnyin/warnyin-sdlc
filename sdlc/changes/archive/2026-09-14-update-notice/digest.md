# Digest — update-notice (deep, shipped 2026-09-14)
- Shipped: once per 24 h, SessionStart records an attempt in `.state/update-check.json` and spawns a detached `check-update.mjs` (minimal env, http(s) only, no redirects, 3 s, 64 KiB). The session never waits. When `sdlc/.hooks/version.json` is older than the cached plain `X.Y.Z`, the next session's context opens with one line outside the budget, pointing to `npx @warnyin/sdlc@latest update` with "do not run it yourself". Off with `updateCheck: false`, `CI` or `NO_UPDATE_NOTIFIER`.
- Specs merged: new `update-notice` (4 requirements, 7 scenarios); Delta command text changed to `@latest` at review. `## Purpose` was filled inside the ship gate.
- Assumptions: notice-only and default-on (human choices); Claude Code only; public registry only (proxy = silence); notice shows from the session after a check. Irreversible once released: `version.json` path, cache shape, `/latest` endpoint, command text.
- Contract: 23 rows, plus row 24 in security-regressions. The contractor's tests had 6/20 rows green before implementation and were rewritten with positive controls. An adversarial pass added future `checkedAt`, symlinked cache, redirect, chunked body and hostile `version.json`.
- Verify: round 1 `mode=panel` passed (5s). Review then found a **blocker**: a symlinked `sdlc/.state/` carried the cache write out of the project. Fixed (realpath check, no mkdir, no request without a recorded attempt) and re-checked by security. Round 2 `mode=panel` passed (5s): `npm test` 303/303, and 4 concurrent copies of the file stayed green. The journal's `verify result=fail round=2` marks that review loop-back; verify itself never failed.
- Other review fixes: `version.json` is always rewritten, since kept copies froze and the notice looped. The checker gets a minimal env, and `@latest` is in the command. CLAUDE.md gained a Hooks network rule and dogfood guidance (`npm run setup:dogfood`, never `npx … update`). The journal's `lines` excludes the notice, and quoted `updateCheck` works.
- Test-quality evidence: a 1500 ms wall-clock bound flaked under load and was replaced by timing against a concurrent opted-out run. A blocking checker still fails it (3084 vs 38 ms). Mutations each turned a test red: redirect, cap, atomic write, future timestamp, CI=false, NO_UPDATE_NOTIFIER, pre-spawn record, patch regex, placement, state-dir guard, version record. One equivalent mutant is accepted: read-time `latest` re-validation.
- Accepted, not done: concurrent sessions may send 2 requests; no CLI to show or force a check; Windows detached spawn untested.
- Pre-authorized escalations (`preauth=yes`): `ship-approval` (deep, hard floor: network egress + registry text into agent context in user projects).
- Cost: shared session · output 392K tokens · cache read 57.8M · cache write 713K · USD not priced.
- Learner proposals (not applied):
  - add-rule (contract.md): deep tier mutation-tests generated contract tests before build. **Second evidence**: release-on-tag's silent guards, then 6 vacuous rows here.
  - tweak (harness/verify): timing-bounded tests must stay green under 4 concurrent copies before verify passes. Evidence: the 1500 ms flake.
  - add-rule (always-loaded, needs /sdlc:steer): writes under `.state/` check realpath containment of the parent, not only the leaf (`lib/active.mjs isRealPathInside`). Evidence: the review blocker.
