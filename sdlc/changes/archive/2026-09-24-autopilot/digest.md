# Digest — autopilot (deep, shipped 2026-09-24)
- **Shipped**: `/sdlc:autopilot` — one up-front grill, then runs to ship deciding by the human's priority order; mandate + per-decision recovery in `grill.md`; `preauth=pilot` journal events; strict validate enforces bullet-per-token, refusals (case-folded, `hardfloor-midrun` covers any unapproved hard-floor flag), same-session delegation; `### Hard floors` is an allowlist. `/sdlc:auto` byte-identical.
- **Specs merged**: new `specs/autopilot/spec.md` (5 requirements); `unattended-run` MODIFIED — ⚠ intended: scenario "an escalation nobody pre-approved" now scoped to `/sdlc:auto`/`--auto` (WHEN clause rewritten), plus a scenario handing the autopilot case over.
- **Assumptions / accepted limits**: `delegation` events and the `hardfloor` flag are agent-recorded — validate holds the run to what was recorded, not that a human spoke or that an unflagged decision touched no hard floor. Sessionless (non-Claude) journals only prove "a delegation was recorded". Human chose "decide everything" for mid-run hard floors, refusable per run.
- **Verify**: 6 fast-gate rounds, final gate full suite 559/560 — the one failure (`kimi adapter … rules-card byte for byte`) fails identically at HEAD (`core.autocrlf=true`); `update-notice` flakes on Windows EPERM, also at HEAD. Evals PASS every round.
- **Review**: 5 panel rounds, all blockers in ONE class — a free-text value compared verbatim let a human's "no" slip (Refused casing → `hardfloor=YES` → `preauth=Pilot` → unbacked `approved` → grill-side denylist). Budget (3) exceeded twice, each time by the human's explicit choice (journaled `answer=`). Round 4 ran 3 of 4 reviewers (quality stalled >600s, `mode=partial`); the final re-check was security-only, targeted (`mode=partial`). No outcome was self-judged (`mode=solo`: none).
- **Pilot decisions**: none (this change was built stage by stage, not under autopilot).
- **Cost**: 74 sessions · 28.6M output · 6.7B cache-read tokens · USD n/a (no price table in `config.yaml`).
- **Learner proposal awaiting you** (not applied — always-loaded): add to constitution "Every free-text value that decides an escalation or refusal SHALL be read in canonical form against a closed set; anything outside it SHALL fail closed." Learner suggested displacing the zero-deps rule — recommend NOT; displace nothing until the budget needs it (constitution is under cap).
- **Notes not fixed**: line counting in `validateGrill` duplicates `caps.mjs`; fenced-bullet asymmetry (fails safe); mixed-tool resume (Claude → other) fails strict — undocumented in the playbook; auto vs autopilot condition names differ in old journals.
- **Freed / waiting**: none.
